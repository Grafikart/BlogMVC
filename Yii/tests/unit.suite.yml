# Codeception Test Suite Configuration

# suite for unit (internal) tests.
# RUN `build` COMMAND AFTER ADDING/REMOVING MODULES.

class_name: CodeGuy
modules:
  enabled: [Yii1, CodeHelper]
  config:
    Yii1:
      appPath: '/srv/http/src/blogmvc/Yii/boot/tests.php'
      url: 'http://blogmvc.yii'
